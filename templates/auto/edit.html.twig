{% extends 'base.html.twig' %}

{% block title %}Edition d'une auto{% endblock %}

 
{% block body %}
     <div class="container mt-5">
        <h1>Modifier l'auto : {{auto.name}}</h1>

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>

        {{ form_end(form) }}
    </div>
{% endblock %}


 